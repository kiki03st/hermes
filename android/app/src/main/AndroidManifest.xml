<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- ACTION_REQUEST_IGNORE_BATTERY_OPTIMIZATIONS가 실제로 다이얼로그를 띄우려면
         이 권한 선언이 필수다(없으면 인텐트가 조용히 아무 효과 없이 끝난다 — 실측 확인). -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- 웨이크워드 감지 시 앱을 안 열고 화면 위에 작은 오버레이만 띄우기 위함(WakeWordOverlay.kt) -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- 헤드리스 음성 답변 도착 시 짧은 진동 알림용(VoiceOutputHelper.kt) -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name=".HermesApplication"
        android:allowBackup="true"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Material.Light.NoActionBar"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 워치가 앱을 켜지 않아도 시스템이 이 서비스를 깨운다 (PLAN.md Stage 2 강제종료 검증 항목) -->
        <service
            android:name=".WatchRelayListenerService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.MESSAGE_RECEIVED" />
                <data
                    android:scheme="wear"
                    android:host="*"
                    android:pathPrefix="/hermes/request" />
            </intent-filter>
        </service>

        <!-- "제우스" 웨이크워드 상시 감지 — 설정 화면 토글에서만 시작/종료된다(다른 프로세스가
             깨우는 WatchRelayListenerService와 달리 exported 불필요). -->
        <service
            android:name=".WakeWordService"
            android:exported="false"
            android:foregroundServiceType="microphone" />
    </application>
</manifest>
